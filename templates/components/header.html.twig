<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">PlanIT</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="ml-2 collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link active" aria-current="page" href="{{ path('projects_index') }}">Projects</a>
            </div>
        </div>

        {% if app.user %}
            <div class="d-flex align-items-center gap-3">
                <button class="btn btn-link text-decoration-none position-relative js-open-invites"
                        data-list-url="{{ path('invites_list') }}"
                        data-count-url="{{ path('invites_count') }}">
                    Invites
                    <span class="badge rounded-pill bg-danger align-middle ms-1 js-invites-count d-none">0</span>
                </button>

                <a href="{{ path('app_logout') }}" style="text-decoration: none;">Logout</a>
            </div>
        {% endif %}

    </div>
</nav>